# Changelog

## 0.9.1
- Hide Login: закрыт обход секретного slug через `dstk_custom_login=1`.
- REST API Control: маршруты редактора только для авторизованных; публично oembed/корень; whitelist capability из allowlist.
- Update Controls: убран вводящий в заблуждение режим `security` (legacy → `minor`).
- RewriteManager: flush rewrite до снятия pending-флага.
- IndexNow: без schedule на каждом front-запросе; `wp_safe_remote_post`.
- Sitemap: debounce инвалидации, узкие hooks; TSF conflict только если TSF реально отдаёт sitemap.
- Disable Comments: блок POST/REST comments; seed новых сайтов сети; uninstall scoped по network_id.
- Добавлены unit-тесты REST, Hide Login bypass, security→minor, initialize_site.

## 0.9.0
- Исправлены блокирующие ошибки сохранения настроек, транслитерации, Hide Login и Update Controls.
- IndexNow переведён на неблокирующую очередь WP-Cron с повторами, защитой от гонок и TSF-aware noindex/canonical правилами.
- Sitemap получил пагинацию, кеширование, корректную поддержку страниц, подкаталогов и plain/index permalinks.
- Добавлены безопасные автообновления только из готовых GitHub Release assets с проверкой SHA-256 и структуры.
- Добавлены адаптивный интерфейс, карточки модулей и отдельная страница помощи.
- Добавлены PHPUnit, security-focused WPCS/PHPCS, PHP compatibility, CI и верификация release ZIP.
- Удалён удалённый JavaScript из wp-admin; блоки поддержки теперь полностью локальные.
- Лицензия синхронизирована как GPL-2.0-or-later.
- Runtime больше не требует Composer на клиенте: при отсутствии `vendor/` используется встроенный PSR-4 loader.
- Уточнены установка и обновления: только ZIP-asset из GitHub Releases, не Code → Download ZIP.

## 0.8.3
- Усилен MVP модуля «REST API Control»: улучшена нормализация whitelist-маршрутов через безопасный разбор пути URL.
- Добавлена дополнительная runtime-санитизация whitelist namespace для защиты от некорректных данных в опции.

## 0.8.2
- Синхронизирована документация с фактическим составом проекта перед первым публичным релизом.
- Подтверждено, что Redirect Manager и Login Attempts не входят в текущий набор реализованных модулей и остаются в roadmap.
- Обновлены релизные версии в коде и readme-файлах.

## 0.8.1
- Выполнен pre-release hardening модуля «Update Controls»: убраны жёсткие bool-типизации входных аргументов filter callbacks, чтобы избежать конфликтов с `null` в нативных фильтрах WordPress.
- В `uninstall.php` добавлена очистка служебной опции кеша `dstk_indexnow_cache`.

## 0.8.0
- Добавлен первый безопасный MVP модуля «Update Controls».
- Добавлены настройки управления автообновлениями ядра (all/minor/security/off).
- Добавлены переключатели автообновлений плагинов, тем, переводов и e-mail уведомлений.
- Реализация построена на нативных WordPress hooks/filters без хака ядра.

## 0.7.1
- Упрощён MVP модуля «Sitemap»: снижена тяжесть выборок и убраны жёсткие проверки noindex-меток сторонних SEO-плагинов.
- Добавлен фильтр `dstk_sitemap_is_noindex` для безопасного расширения правил исключения noindex.
- Уточнён TSF-safe режим через интеграционный детектор: sitemap Dr.Slon Toolkit не активируется, если TSF обслуживает sitemap.

## 0.7.0
- Добавлен первый безопасный MVP модуля «Sitemap».
- Добавлены настройки модуля Sitemap: отдельное включение runtime, выбор типов записей и таксономий.
- Реализована отдача XML sitemap index (`/sitemap.xml`) и отдельных sitemap сущностей (`/sitemap-pt-{post_type}.xml`, `/sitemap-tax-{taxonomy}.xml`).
- Для записей добавлен `lastmod`, если дата изменения доступна без тяжёлых операций.
- Добавлен TSF-safe режим: при активном The SEO Framework runtime sitemap Dr.Slon Toolkit отключается, а в админке показывается понятное уведомление.

## 0.6.1
- Выполнен hardening MVP модуля «IndexNow».
- Усилен runtime отдачи ключа `/<key>.txt` (GET/HEAD, предсказуемый ответ без лишнего вывода).
- Добавлен runtime-whitelist endpoint с безопасным fallback.
- Улучшен антидубль отправок URL для снижения конфликтов между ручной и автоматической отправкой.

## 0.6.0
- Добавлен первый безопасный MVP модуля «IndexNow».
- Добавлена ручная отправка URL из админки с nonce и проверкой прав.
- Добавлена автоотправка URL при публикации/обновлении/перемещении в корзину/удалении поддерживаемых типов записей.
- Добавлена безопасная выдача проверочного ключа IndexNow по адресу `/<key>.txt`.

## 0.5.0
- Выполнен hardening MVP модуля «REST API Control».
- Добавлен встроенный неизменяемый базовый allowlist системных маршрутов WordPress.
- Пользовательское поле системных маршрутов теперь только расширяет allowlist и не может разрушить базовые маршруты.
- Добавлены безопасные префиксные маршруты без regex для критичных сценариев редактора и медиа.

## 0.3.0
- Добавлен первый безопасный MVP модуля «REST API Control».
- Добавлены режимы управления доступом к REST API и whitelist маршрутов/namespace.
- Добавлена настройка доверенной capability и отдельный список системных маршрутов.

## 0.2.0
- Добавлена рабочая модульная архитектура с автозагрузкой Composer PSR-4.
- Добавлены центральный запуск плагина, обработчик активации и очистка при uninstall.
- Добавлена нативная страница настроек с переключателями модулей и параметрами очистки.
- Реализованы модули «Транслитерация», «Отключение комментариев» и «Очистка».
- Добавлен MVP модуля «Скрытый вход» с кастомным slug, защитой wp-login.php и аварийным bypass через `KRV_DSTK_DISABLE_HIDE_LOGIN`.
- Добавлено уведомление о совместимости с The SEO Framework.
- Улучшено объединение вложенных настроек и обработка сложных случаев slug при транслитерации.
- Снижены побочные эффекты в модулях отключения комментариев и очистки.
